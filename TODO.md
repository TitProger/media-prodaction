# TODO — Content Factory

## Баги / критические исправления

- [x] **Автодетект видео-баннера в UI и CLI** — логика перенесена в `video_composer.compose()`,
  определяет тип по расширению файла автоматически.

- [ ] **Лимит 50 МБ в Telegram** — `send_video` падает с ошибкой на файлах >50 МБ.
  Добавить проверку размера `output.mp4` перед отправкой и предупреждать пользователя
  (или отправлять как `send_document`).

## Качество видео / обрезка

- [ ] **Улучшить обрезку вертикальных видео (9:16)**
  Сейчас вертикальный 1080×1920 кадр теряет 480px сверху и снизу — видна только середина.
  Варианты:
  - `pad` вместо `crop` (чёрные полосы, но ничего не обрезается)
  - Настраиваемое смещение crop_y (параметр в UI)
  - Авто-выбор стратегии по AR входного видео

- [ ] **Настраиваемая позиция кропа** — добавить в Gradio слайдеры
  `top_crop_x / top_crop_y` и `bot_crop_x / bot_crop_y`, чтобы пользователь
  сам выбирал, какую часть кадра оставить.

## Субтитры

- [ ] **Word-level тайминги через Whisper** — сейчас тайминги интерполируются по
  отрезку сегмента. Включить `word_timestamps=True` в `model.transcribe()` и
  итерировать по `segment["words"]` — каждое слово получит точный `start/end`.

- [ ] **Karaoke-подсветка текущего слова** — популярный TikTok/Shorts стиль:
  текущее слово выделяется другим цветом. Реализуется через ASS-теги `{\k<время>}`
  (нужны word-level timestamps из пункта выше).

- [ ] **Выбор языка в Telegram-боте** — сейчас язык транскрипции задаётся только
  в `settings.py`. Добавить шаг в диалог: пользователь пишет код языка (`ru`, `en`, …)
  или «авто».

## UX / интерфейс

- [ ] **Прогресс FFmpeg в реальном времени** — запустить FFmpeg с флагом `-progress pipe:1`,
  парсить `out_time=` строки и обновлять прогресс-бар в Gradio вместо статичного
  «Рендерю видео…».

- [ ] **Очистка папки `output/`** — каждый запуск оставляет новую подпапку, они
  никогда не удаляются. Добавить TTL (например, удалять файлы старше 24 часов)
  или кнопку «Очистить» в Gradio.

- [ ] **Предпросмотр кропа перед рендером** — показывать в Gradio статичный кадр
  (через FFmpeg `-frames:v 1`) как будет выглядеть обрезка, до запуска полного рендера.

## Архитектура

- [ ] **Вынести загрузку модели Whisper** — сейчас `whisper.load_model()` вызывается
  при каждом запуске пайплайна. Загружать один раз при старте приложения и
  переиспользовать (кешировать в `app.py` / `bot.py`).

- [ ] **Поддержка нескольких аудио-дорожек** — сейчас аудио берётся только из
  верхнего видео (`-map 0:a`). Добавить опцию микшировать аудио из обоих видео
  с регулировкой громкости.
